namespace ZaloCommunityDev.Shared
{
    public static class Helper
    {
        public static GenderSelection[] GenderSelectionList = new[]
        {
            GenderSelection.OnlyMale,
            GenderSelection.OnlyFemale,
            GenderSelection.OnlyMale,
        };
    }
}